[tool.ruff]
target-version = "py311"
line-length = 100
extend-exclude = [
  "_refs",
  "admin",
  "data",
  "logs",
]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]

[tool.ruff.lint.per-file-ignores]
# scripts 下允许通过 sys.path hack 引入 app（用于本地脚本）
"scripts/*.py" = ["E402"]

